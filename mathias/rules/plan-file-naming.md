---
paths:
  - "**/plans/*.md"
---

# Plan File Naming

Plan filenames must be descriptive kebab-case `<verb>-<object>.md` (e.g. `add-csv-export.md`, `refactor-validation-pipeline.md`).

Never use the harness's auto-generated random adjective-noun suffixes (`-crispy-hippo`, `-distributed-peach`, etc.) — override the suggested path before writing.

Plans live in `~/dev/projects/ifas13-agents/mathias/plans/`.